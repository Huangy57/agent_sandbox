# Files to hide (overlay with /dev/null) for Claude Code.
# The real CLAUDE.md is hidden so the agent only sees the merged
# sandbox-config copy (via CLAUDE_CONFIG_DIR).

AGENT_HIDE_FILES=(
    "\$HOME/.claude/CLAUDE.md"
)
