#!/bin/bash --
# Sandbox salloc — shadows /usr/bin/salloc inside the sandbox.
# Delegates to chaperon stub which blocks the command.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/salloc" "$@"
