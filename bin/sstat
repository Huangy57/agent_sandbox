#!/bin/bash --
# Sandbox sstat — shadows /usr/bin/sstat inside the sandbox.
# Delegates to chaperon stub which validates job scope before passthrough.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/sstat" "$@"
