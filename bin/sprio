#!/bin/bash --
# Sandbox sprio — shadows /usr/bin/sprio inside the sandbox.
# Delegates to chaperon stub which injects user scope before passthrough.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/sprio" "$@"
