#!/bin/bash --
# Sandbox sinfo — shadows /usr/bin/sinfo inside the sandbox.
# Delegates to chaperon stub which validates flags before passthrough.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/sinfo" "$@"
