#!/bin/bash --
# Sandbox scrontab — shadows /usr/bin/scrontab inside the sandbox.
# Delegates to chaperon stub which blocks the command.
_BIN_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "$_BIN_DIR/../chaperon/stubs/scrontab" "$@"
